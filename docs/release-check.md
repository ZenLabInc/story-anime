# 公開版の確認（2026-09-15）

- GitHub公開後、認証なしのHTTPSクローンに成功。公開履歴は新規コミットから始まり、旧非公開履歴は公開リポジトリ経由で取得できないことを確認。
- クローンしたコードで --local 起動、healthz、アカウント、画風見本5枚のHTTP 200と課金・クラウド認証の無効を確認。
- 新規フォルダ・Python 3.14仮想環境で依存ライブラリをインストールして --local 起動を確認。
- Chromeで初回案内、プロフィール登録、新規漫画作成、APIキー未登録時の停止を確認。
- Python 160テスト成功。会話・画像のプロバイダー部分はモックであり、この移行での実生成品質の再評価ではない。
- JSルーティング、送信ショートカット、Markdown、音声認識のモックテスト成功。
- Gitleaksで旧履歴と公開用ファイルを走査。テスト用ダミー値の検出は確認し、公開版では実際の秘密値を検出していない。
- 会社の連絡先、事業計画、運用記録、クラウド識別子、フォーム・回答シートの管理URL、旧Git履歴を公開ファイルから除外。
- 画風見本はローカル同梱。会社サーバーへの接続は不要。
- 旧クラウドの作品をバックアップし復元検証済み。専用AWSスタック、サーバー、固定IP、S3全バージョン、CloudFront、メールIAM、SSMと旧DNSを撤去。

## 限界

秘密情報の自動走査はあらゆる漏洩を保証するものではありません。依存ライブラリやGoogleのモデル提供条件は変わります。WindowsはWSL2を利用し、Windowsネイティブ起動は未対応。今回の新規環境検証はmacOS/Python 3.14で行い、Ubuntu/WSL実機での検証はしていません。実APIの画像品質、ブラウザの実マイク、長編の品質はこの移行で追加評価していません。
